When we initiate a repo using git init a .git directory is place inside the rep which contains all the meta data for the history of previous versions of the file.

It is because staging area helps me to control one by one how many files i want to commit at once instead of commiting all the files at once or each file one by one.

By adding the files that make one logical change